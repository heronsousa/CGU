4+4/5
(5-3)^3

# 'c()' concatena valores em um vetor
x = c(4,5,6)

c(4,5,6) -> y

y

# assign(variavel q ira receber valores, valores)
assign('x', c(4.2, 1, 5))

x = c(1, 2, 3); x

# Mostra objetos criados
ls()
objects()

# Remove objeto
rm("myfirstobject")

x = c(y, 5, y); x

sum(x)

sqrt(x)

# Acessar valor do vetor por index
x[1]

# Verifica no vetor x quais numeros são menores do que 5 e atribui TRUE ou FALSE a variavel 'newobject'
newobject <- x < 5; newobject
x